function preds = getPreds(node)

%technically all neighbors are preds

pred_count=1
for i=-1:1:1
    for j=-1:1:1
        pred_x = node.x + i;
        pred_y = node.y + j;
        if( ~(pred_x == 0 && pred_y == 0) &&  ) %if not current
        preds(predcount) = node_Grid(i,j)




end