function foundChanges = scanForChanges()
%scan through the graph, find nodes that have changed, add them to a list
%for updating

foundChanges = 0; %default to not having found any changes

%subtract grids from each other, anything non-zero is a changed node and
%should be added to the list for processing

%to start - test the rest of the algo first with this always returning 0

end
